name: Release
on:
  push:
    branches: [main]

concurrency:
  group: release
  cancel-in-progress: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Publish Release
        uses: robgonnella/releasaurus/action@c51fedaf4cdbfa7af439e91f595146c7a9ffa6bf # v1.2.2
        with:
          command: release
          command_args: >-
            --forge github
            --repo ${{ github.server_url }}/${{ github.repository }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create Release PR
        uses: robgonnella/releasaurus/action@c51fedaf4cdbfa7af439e91f595146c7a9ffa6bf # v1.2.2
        with:
          command: release-pr
          command_args: >-
            --forge github
            --debug
            --repo ${{ github.server_url }}/${{ github.repository }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
